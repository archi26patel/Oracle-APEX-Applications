  CREATE TABLE "TA_MEETINGS" 
   (	"MEETING_ID" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"MEETING_TITLE" VARCHAR2(200) NOT NULL ENABLE, 
	"MEETING_DESC" VARCHAR2(1000), 
	"START_DATETIME" TIMESTAMP (6) NOT NULL ENABLE, 
	"END_DATETIME" TIMESTAMP (6) NOT NULL ENABLE, 
	"ORGANIZER_ID" NUMBER NOT NULL ENABLE, 
	"MEETING_TYPE" VARCHAR2(30), 
	"MEETING_LOCATION" VARCHAR2(300), 
	"STATUS" VARCHAR2(20) DEFAULT 'SCHEDULED', 
	"CREATED_ON" DATE DEFAULT SYSDATE, 
	"CREATED_BY" VARCHAR2(500), 
	 PRIMARY KEY ("MEETING_ID")
  USING INDEX  ENABLE
   ) ;

  ALTER TABLE "TA_MEETINGS" ADD CONSTRAINT "FK_MEETING_ORGANIZER" FOREIGN KEY ("ORGANIZER_ID")
	  REFERENCES "TA_EMP" ("ID") ENABLE;

  CREATE OR REPLACE EDITIONABLE TRIGGER "TA_MEETINGS_BIU" 
    before insert or update 
    on TA_MEETINGS 
    for each row 
begin 
    if inserting then 
        :new.CREATED_ON := sysdate; 
        :new.CREATED_BY := coalesce(sys_context('APEX$SESSION','APP_USER'),user); 
    end if; 
end TA_MEETINGS_BIU;
/
ALTER TRIGGER "TA_MEETINGS_BIU" ENABLE;