  CREATE TABLE "AOP_DEMO_EMPLOYEE_MASTER" 
   (	"EMP_ID" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"EMP_CODE" VARCHAR2(20), 
	"FIRST_NAME" VARCHAR2(100), 
	"LAST_NAME" VARCHAR2(100), 
	"EMAIL" VARCHAR2(200), 
	"MOBILE" VARCHAR2(20), 
	"GENDER" VARCHAR2(20), 
	"DOB" DATE, 
	"DESIGNATION" VARCHAR2(100), 
	"DEPARTMENT" VARCHAR2(100), 
	"JOINING_DATE" DATE, 
	"EMPLOYMENT_TYPE" VARCHAR2(50), 
	"SALARY" NUMBER(12,2), 
	"REPORTING_MANAGER" VARCHAR2(100), 
	"COMPANY_NAME" VARCHAR2(200), 
	"COMPANY_ADDRESS" VARCHAR2(500), 
	"COMPANY_EMAIL" VARCHAR2(200), 
	"COMPANY_PHONE" VARCHAR2(30), 
	"HR_NAME" VARCHAR2(100), 
	"STATUS" VARCHAR2(20), 
	"TEMPLATE_ID" NUMBER, 
	"CREATED_ON" DATE DEFAULT SYSDATE, 
	"CREATED_BY" VARCHAR2(255), 
	"UPDATED_ON" DATE, 
	"UPDATED_BY" VARCHAR2(255), 
	 CONSTRAINT "PK_EMPLOYEE" PRIMARY KEY ("EMP_ID")
  USING INDEX  ENABLE, 
	 UNIQUE ("EMP_CODE")
  USING INDEX  ENABLE
   ) ;

  ALTER TABLE "AOP_DEMO_EMPLOYEE_MASTER" ADD CONSTRAINT "FK_EMP_TEMPLATE" FOREIGN KEY ("TEMPLATE_ID")
	  REFERENCES "AOP_DEMO_LETTER_TEMPLATE_MASTER" ("TEMPLATE_ID") ENABLE;

  CREATE OR REPLACE EDITIONABLE TRIGGER "TRG_EMPLOYEE_CODE" 
BEFORE INSERT
ON AOP_DEMO_EMPLOYEE_MASTER
FOR EACH ROW
BEGIN
    -- Generate EMP_CODE only if not provided
    IF :NEW.EMP_CODE IS NULL THEN
        :NEW.EMP_CODE := 'EMP' || LPAD(:NEW.EMP_ID, 4, '0');
    END IF;
END;
/
ALTER TRIGGER "TRG_EMPLOYEE_CODE" ENABLE;